const numbers = [1, 2, 3, 4, 5];

/**
 * map: Transforms array elements with a provided function, creating a new array.
 */

// const doubledNumbers = numbers.map((number, arr, index) => {
//   console.log(index);
//   console.log(arr);
//   return number * 2;
// });

// console.log(doubledNumbers);

/**
 * filter: Creates a new array with elements that satisfy a specified condition.
 */

// const doubledNumbers = numbers.filter((num) => {
//   return num % 2 === 0;
// });

// console.log(doubledNumbers);

// const accumulatedNumbers = numbers.reduce((total, sum) => {
//   return total + sum;
// }, 0);

// console.log(accumulatedNumbers);

/**
 * reduce: Accumulates array elements into a single value using a provided function.
 */

/**
 * forEach: Iterates through array elements and applies a function without creating a new array.
 */

// for each is short hand of the for loop and

// numbers.forEach((num) => {
//   console.log(num);
// });

/**
 * find: Returns the first array element that satisfies a specified condition.
 */

// find will give you the first element that satisfies the given condition

// const foundNumber = numbers.find((num) => num < 4);
// console.log(foundNumber);

/**
 * some: Checks if at least one array element satisfies a condition.
 */

// const hasEvenNumber = numbers.some((num) => num > 14);

// console.log(hasEvenNumber);

/**
 * every: Checks if all array elements satisfy a condition.
 */
